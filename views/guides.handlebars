<div class="container">
    <div class="row">
        <div class="col-12">
            <p>
                <a href="/guides/popular" class="text-dark">Guides</a>
            </p>
        </div>
        <div class="col-md-3 mb-1">
            <select id="selectFilter" class="form-control form-control-sm">
                <option value="popular">Popular</option>
                <option value="recent" selected>Recent</option>
            </select>
        </div>
        <div class="col-md-7">
            <div class="input-group mb-1">
                <input type="text" class="form-control form-control-sm align-bottom" id="searchInput" aria-describedby="SearchPost"
                    placeholder="Search...">
                <div class="input-group-append">
                    <button class="btn btn-outline-secondary btn-sm" type="button"><span class="mdi mdi-magnify"></span></button>
                </div>
            </div>
        </div>
        <div class="col-md-2">
            {{#if user}}
            <a href="/create-guide" class="btn btn-success btn-sm btn-block mb-3 float-right"><span class="mdi mdi-plus"></span>
                Create</a>
            {{else}}
            <button class="btn btn-success btn-sm btn-block mb-3 float-right" type="button" data-toggle="modal"
                data-target="#loginModal"><span class="mdi mdi-plus"></span>
                Create</button>
            {{/if}}

        </div>
        <div class="col">
            {{#unless guides}}
            <p class="text-center">No results found 😔</p>
            {{else}}
            <div class="table-responsive">
                <table class="table table-hover">
                    <tbody>
                        {{#each guides}}
                        <tr>
                            <th class="font-weight-normal align-middle"><a href="/guide/{{_id}}">{{title}}</a></th>
                            <th class="text-muted small text-left align-middle"><span class="mdi mdi-eye"></span>
                                {{viewCount}}</th>
                            <th class="text-muted small text-left align-middle"><span class="mdi mdi-thumb-up">
                                    {{voteCount}}</span></th>
                            <th class="text-muted small text-right align-middle"><a class="text-muted" href="#">{{author}}</a><br>
                                <time class="time" title="{{{date_create}}}"></time>
                            </th>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
                <nav aria-label="Page navigation">
                    <ul class="pagination pagination-sm justify-content-center" id="page_nav">
                    </ul>
                </nav>
            </div>
            {{/unless}}
        </div>
    </div>
</div>
<script>
    'use strict'
    var page = {{{ page }}}
    var totalPages = {{{ totalPages }}}
    var filter = '{{{reqFilter}}}'
    var pagename = 'guides'
</script>
<script src="/js/ms2w_posts.js"></script>